name: Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: Install dependencies
      run: poetry install --no-root
    - name: Test with pytest
      run: make coverage.xml
